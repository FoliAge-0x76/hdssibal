# 高清西八杯（HD Ssibal Cup）在线比赛会场 —— 需求报告

> 版本：**v1.1（定稿）** —— 第 17 章 16 项决策已于 2026-09-15 全部拍板；同日二次修订：**投稿形态由「三件套」改为「封面图 + 公开谱面外链」**（见 17.1）；同日三次追加：**外链校验改为「域名白名单」机制**（见 17.12～17.16）
> 撰写日期：2026-09-15
> 本文档只描述需求与技术选型，**不含任何代码**。
> 文中所有「免费额度 / 平台现状」数据均为 2026-09-15 检索核实结果，来源见附录 A；此类数字变动频繁，落地前建议重新核对一次。

---

## 1. 项目背景与目标

### 1.1 背景

高清西八杯（HD Ssibal Cup）是一个**音游谱面投稿与公众评分比赛**。目前大多校级民间赛事的投稿收集、作品展示、评分统计依赖人工（群文件 / 表格 / 手工计分），存在三个问题：

1. **作品分散**：投稿散落在聊天记录与网盘中，选手和观众都无法一览全部参赛作品。
2. **评分靠人力**：投票与统分需要人工汇总，容易出错，也无法防止重复投票。
3. **没有沉淀**：往届作品与结果无法长期留存与检索。

### 1.2 目标

建设一个**由 GitHub Pages / Cloudflare Pages 一类静态托管承载的在线比赛会场**，实现：

- 观众与选手可**用 GitHub 账户登录**；
- 选手可**上传 / 修改自己的作品**（封面图 + 谱面下载外链）；
- 主页展示**最近的活动**；
- 活动页以**封面图 + 标题 + 作者**的卡片墙展示全部已投稿作品；
- 观众对作品**评分（1–5 星）**，系统按**简单平均分**实时生成排行榜；
- **管理员**可添加活动、删除作品、下架违规内容、授予他人管理员权限；
- 支持**多届长期运营**，往届可翻查归档。

### 1.3 设计原则

| 原则 | 含义 |
|---|---|
| 零服务器运维 | 不承担自建服务器的运维、备案与安全责任，全部依赖托管服务免费额度 |
| 内容与代码分离 | 活动、作品等业务数据存于后端数据库，不随前端代码一起构建发布（避免每次投稿都触发构建） |
| 权限下沉到数据库 | 前端只做展示与交互，真正的权限判定由数据库行级安全策略（RLS）执行，避免前端绕过 |
| 渐进增强 | 先做能跑通的最小闭环（登录→投稿→评分→排行），再补通知、评论等增强项 |
| 成本可控 | 单届规模为校内小型赛（数十件作品、数百名投票者），选定方案的免费额度需有数量级余量 |

---

## 2. 名词定义

| 术语 | 含义 |
|---|---|
| 活动 / Event | 一届比赛。同一时间可能有多个活动共存（例如「2026 春季赛」与「2026 夏季赛」） |
| 报名 / Registration | 选手在「报名期」内主动登记，取得该届的**投稿资格** |
| 投稿 / Work | 一件参赛作品，包含标题、作者、封面图、谱面下载外链、简介 |
| 谱面外链 / Chart Link | 选手自备的、公开可访问的谱面下载地址（http/https）。平台**不代存谱面文件**，只保存并展示该链接；其主机名必须落在**外链域名白名单**内 |
| 外链域名白名单 / Link Domain Allowlist | 平台允许收录的托管平台域名清单（GitHub Release、对象存储直链、主流网盘等）。投稿时外链主机名必须命中该清单才能提交（见 17.12、8.9） |
| 提取码 / 分享密码 | 部分网盘分享页下载所需的短口令。仅当白名单把该域名标记为「需密码」时才要求填写（见 17.14） |
| 评分 / Rating | 登录用户对某件作品给出的 1–5 星分值 |
| 榜单 / Leaderboard | 按作品平均分降序排列的列表 |
| 时间窗 / Window | 由 5 个时间戳切分出的 4 个阶段：报名期、投稿期、评分期、公示期 |

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 取得方式 | 说明 |
|---|---|---|
| **访客**（匿名） | 无需登录 | 只能浏览公开内容 |
| **登录用户** | GitHub OAuth 一次授权 | 可评分、可评论 |
| **报名选手** | 登录后于报名期内主动报名 | 具备投稿资格；报名本身不排斥其评分权 |
| **管理员** | 由已管理员在后台授予（数据库角色表） | 建活动、删作品、下架、管理角色 |

> 角色可叠加：管理员若同时报名，也具备选手权限。

### 3.2 权限矩阵

| 操作 | 访客 | 登录用户 | 报名选手 | 管理员 |
|---|:--:|:--:|:--:|:--:|
| 浏览主页 / 活动列表 / 往届归档 | ✅ | ✅ | ✅ | ✅ |
| 查看活动页作品墙 | ✅ | ✅ | ✅ | ✅ |
| 查看作品详情（封面、简介、谱面外链入口） | ❌ | ✅ | ✅ | ✅ |
| 查看排行榜 | ✅ | ✅ | ✅ | ✅ |
| **报名** | ❌ | ✅ | — | ✅ |
| **投稿**（新建） | ❌ | ❌ | ✅ | ✅ |
| **修改自己的作品** | ❌ | ❌ | ✅（仅本人，投稿截止前） | ✅（任意，不受时限） |
| **删除自己的作品** | ❌ | ❌ | ✅（仅本人，投稿截止前） | ✅（任意） |
| **评分** | ❌ | ✅ | ✅ | ✅ |
| **修改自己的评分** | ❌ | ✅ | ✅ | ✅ |
| **发表评论** | ❌ | ✅ | ✅ | ✅ |
| **创建 / 编辑活动与时间窗** | ❌ | ❌ | ❌ | ✅ |
| **删除 / 下架任意作品** | ❌ | ❌ | ❌ | ✅ |
| **管理外链域名白名单**（增删托管平台、标记是否需提取码） | ❌ | ❌ | ❌ | ✅ |
| **授予 / 撤销管理员** | ❌ | ❌ | ❌ | ✅ |
| **导出某届结果（CSV）** | ❌ | ❌ | ❌ | ✅ |

> **作品详情页需登录**（见 17.8）：作品墙与排行榜对访客开放，但点进作品详情需要 GitHub 登录。这样既能靠公开的作品墙做传播，又能让深层交互（下载、评分、评论）统一留在登录态后，便于统计与治理。

### 3.3 身份来源

- 登录**只提供 GitHub OAuth 一种方式**（需求已明确放弃 QQ 登录）。
- 参考资料：[giscus](https://github.com/giscus/giscus) 就是「GitHub OAuth + GitHub 数据源」的成熟范式；本项目采用 **Supabase Auth 内置的 GitHub Provider** 达到同样的效果，无需自建 OAuth 中转层（详见 10.2）。
- 首次登录时自动落库一条 `profiles` 记录（含 GitHub 用户名、头像 URL）；默认角色为普通用户，**不存在任何默认可获得管理员权限的路径**。

---

## 4. 业务流程与时间模型

### 4.1 五个时间点（用户确认）

一届活动由 5 个时间戳界定：

| 序号 | 字段（概念名） | 含义 |
|---|---|---|
| ①  | `signup_opens_at` | **报名开始** |
| ②  | `submit_opens_at` | **投稿开始**（同时开启评分入口） |
| ③  | `signup_closes_at` | **报名结束**（之后不能再报名，已报名者仍可投稿） |
| ④  | `submit_closes_at` | **投稿截止**（不能再新建投稿，本人也不可再修改，见 17.10） |
| ⑤  | `rating_closes_at` | **评分截止**（之后锁定评分，进入结果公示） |

### 4.2 由时间点派生的活动状态

| 状态 | 判定条件 | 用户可见文案 |
|---|---|---|
| 预告 | 当前 < ① | 「即将开始」 |
| 报名中 | ① ≤ 当前 < ③ | 「报名中 · 剩余 X 天」 |
| 投稿+评分中 | ③ ≤ 当前 < ④ | 「投稿进行中 · 评分进行中」 |
| 评分中 | ④ ≤ 当前 < ⑤ | 「投稿已截止 · 评分进行中」 |
| 已结束 | 当前 ≥ ⑤ | 「已结束 · 查看结果」 |

> 状态是**由时间戳实时计算**的派生值，不单独存字段（避免定时任务改状态的复杂度）。

### 4.3 端到端流程

```
访客浏览主页（最近活动）
   │
   ├─ 点击活动 → 活动页：作品墙（封面 + 标题 + 作者）+ 排行榜入口
   │
   └─ 点击作品 → 作品详情页：封面、简介、谱面外链跳转、评论区、评分控件
             │
             └─ 未登录 → 触发 GitHub 登录 → 回到原页面

选手（已登录）：
   报名期 → 报名 → 「我要投稿」→ 上传封面图 + 填写谱面下载外链（必要时填提取码）
            → 勾选原创声明 → 自动校验（含外链域名白名单校验）→ 立即公开
   投稿期内可反复编辑自己的作品

观众（已登录）：
   评分期内 → 打开作品 → 打 1–5 星 → 写入评分（同一作品可修改）

活动结束后：
   榜单锁定 → 主页「往届赛果」归档入口 → 可查看该届作品与排名
```

### 4.4 关键业务规则

1. **只有报名选手能投稿**；**评分对所有登录用户开放**（不必报名）。
2. 投稿**免人工审核**，但必须先通过**自动校验**（见 6.3）。
3. 同一用户对**同一作品只有一条评分**，重复评分视为「修改」。
4. 排名规则：**简单平均分**，降序；平均分相同者按「评分数更多」优先，再相同按投稿时间更早优先。
5. 未获得任何评分的作品在榜单中单独归入「暂无评分」分组，不参与名次。
6. 投稿必须提供**封面图**与**公开可访问的谱面下载外链**（http/https），且外链主机名必须**命中平台域名白名单**，缺任一项不可提交（详见 6.3）。平台**不代存谱面文件**，以减轻在线存储压力。
7. **投稿截止（④）后，作品完全锁定**：选手本人也不可再修改或删除；仅管理员可下架或做例外处理，且必须写入审计日志。
8. **评分截止（⑤）后，评分完全锁定**：所有评分记录不可再增删改。

---

## 5. 页面与信息架构

### 5.1 站点地图

| 路由 | 页面 | 需要登录 | 主要内容 |
|---|---|:--:|---|
| `/` | 主页 | ❌ | Hero 区、**最近的活动**（进行中优先，最多 3 个）、往届归档入口、平台规则入口 |
| `/events` | 活动列表 | ❌ | 全部活动，按进行中 / 已结束分组 |
| `/events/:slug` | 活动页 | ❌ | 活动说明、倒计时、时间轴、**作品墙**、排行榜入口、投稿按钮（按权限显隐） |
| `/events/:slug/leaderboard` | 排行榜 | ❌ | 名次、封面、标题、作者、平均分、评分数 |
| `/works/:id` | 作品详情 | ✅ | 封面大图、标题、作者、简介、谱面外链下载入口、评分控件、评论区 |
| `/events/:slug/submit` | 投稿页 | ✅ | 见 6.3 投稿表单 |
| `/me/works` | 我的作品 | ✅ | 我的投稿列表，可编辑 / 删除 |
| `/me/ratings` | 我的评分 | ✅ | 我给过的评分，可修改 |
| `/admin` | 管理后台 | ✅ 管理员 | 活动管理、作品管理、角色管理、**外链域名白名单管理**、结果导出 |
| `/about` | 投稿须知 | ❌ | 版权与原创声明、社区规范、免责条款、**已放行托管平台清单与推荐托管教程**（见 17.15） |
| `404` | 未找到 | ❌ | 引导回主页 |

### 5.2 主页分区（需求 3：显示最近的活动）

1. **Hero**：赛事名、一句话简介、当前进行中的活动倒计时。
2. **最近的活动**：卡片列表，每张卡含活动封面、标题、状态徽章、时间窗、作品数、「进入会场」按钮。
3. **往届赛果**：最近 3 届已结束活动的入口。
4. **我要投稿**：明确的行动号召，未登录时点击即触发 GitHub 登录。

### 5.3 活动页作品墙（需求 4：封面图 + 标题 + 作者）

- 网格卡片布局，**每张卡严格包含三要素**：封面图、标题、作者名（附头像）。
- 卡片可扩展小角标：平均分、评分数、状态（如「已下架」遮挡）。
- 卡片点击进入作品详情（**详情页需登录**，见 17.8）。
- 排序：默认按投稿时间倒序；可切换为按平均分降序。
- 分页 / 无限滚动（每页 24 件）。
- 空状态：「本届还没有作品，第一个投稿吧」。
- **不做**关键词搜索与多维筛选（需求已明确排除）。

---

## 6. 功能需求

### 6.1 认证与会话（FR-1）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-1.1 | 支持 GitHub OAuth 一键登录，无需用户设置密码 | P0 |
| FR-1.2 | 登录后保持会话（刷新页面不掉登录） | P0 |
| FR-1.3 | 首次登录自动创建用户档案（GitHub 名、头像） | P0 |
| FR-1.4 | 可登出，登出后受保护路由自动跳转登录 | P0 |
| FR-1.5 | 登录成功 / 失败 / 取消授权时给出明确提示 | P1 |
| FR-1.6 | 前端不持有任何可越权调用后端的密钥（只用可公开的匿名密钥） | P0 |

### 6.2 活动与报名（FR-2）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-2.1 | 主页展示最近的活动（卡片形态、含状态） | P0 |
| FR-2.2 | 活动列表页按「进行中 / 已结束」分组 | P0 |
| FR-2.3 | 活动页展示 5 个时间点构成的时间轴与实时状态 | P0 |
| FR-2.4 | 活动页展示活动规则说明（富文本 / Markdown） | P1 |
| FR-2.5 | 报名期内登录用户可一键报名，报名后 UI 显示「已报名」 | P0 |
| FR-2.6 | 报名期外无法报名（前端隐藏 + 后端强制拒绝） | P0 |
| FR-2.7 | 报名不可自行撤销（需联系管理员），避免刷名额误操作 | P2 |
| FR-2.8 | 往届活动可无限期翻查，作品与榜单保持锁定 | P0 |

### 6.3 投稿（FR-3）—— 本次重点

**投稿表单字段**

| 字段 | 必填 | 说明 |
|---|:--:|---|
| 参赛活动 | ✅ | **下拉框，只列出「当前正在投稿」的活动**（即状态为「投稿+评分中」的活动）；若一个都没有，投稿入口整体禁用并提示。**单选**——一件作品只能投一个活动（见 17.2） |
| 作品标题 | ✅ | 2–60 字 |
| 作者显示名 | ✅ | 默认取 GitHub 名，可改为昵称 |
| 封面图 | ✅ | 一张图片，**对文件名无要求**；限 `png` / `jpg` / `jpeg` / `webp`，单文件 ≤ 1 MB，建议比例 16:9。**这是平台唯一需要承载的投稿文件**（见 17.16） |
| 谱面下载外链 | ✅ | **公开可访问的 http/https 绝对 URL**，指向谱面文件的下载地址，且**主机名必须命中域名白名单**（GitHub Release / raw、对象存储直链、主流网盘分享页等，见 8.9、17.12）。平台**不代存谱面文件**，只保存并展示该链接 |
| 提取码 / 密码 | ⚠️ | **选填**；仅当外链所在域名被白名单标记为「需密码」时变为**必填**（如百度网盘、带密码的蓝奏云分享页，见 17.14）。1–16 位，非敏感信息 |
| 作品简介 | ❌ | ≤ 300 字，纯文本 |
| 原创与授权声明 | ✅ | **复选框，必须勾选**才能提交（见 6.9） |

**表单行为**

- **封面图与谱面外链两件齐备**（外链域名需在标记为「需密码」时同时填好提取码），再加上活动选择、标题、声明全部就绪后，**提交按钮才可点击**；缺任一项按钮置灰，并逐项列出缺失原因。
- 封面图**选中后立即就地校验**（扩展名 → 文件头 → 体积），不通过则当场标红说明原因。
- 谱面外链**在输入框失去焦点时立即校验**：先做 URL 语法与 scheme 检查，再提取主机名比对白名单。**命中即就地回显绿色「该托管平台已支持」**；未命中则标红，并**在同一位置列出已放行平台清单与「申请添加托管平台」入口**（见 17.15）。**整个校验过程不发任何网络请求。**
- 白名单域名的「需密码」标记决定提取码字段的呈现：标记为需密码时字段出现且**必填**；否则保持选填（默认隐藏，提供「有提取码？」展开链接）。
- 提交前做**前端预校验**，提交后由**后端再做一次强校验**（前端校验不可信）。后端对封面图做**三重判断**：**扩展名 + 文件头（magic bytes）+ MIME**，三者一致才放行；对外链做 URL 语法、scheme 与**域名白名单**的二次校验（后端直查 `link_domains` 表，不信任前端传来的判定结果），并按域名标记强制校验提取码。**后端全程不访问该外链。**
- 提交成功后跳转到自己的作品详情页，并触发**投稿成功邮件**（见 6.8）。
- 投稿期内本人可**编辑**自己的作品（更换封面 / 修改外链 / 改标题与简介），编辑同样走完整校验。
- **投稿截止后完全锁定**：本人也无法再修改或删除自己的作品，保证公平（见 17.10）。
- **不允许**非报名者投稿，**不允许**跨届投稿，**不允许**代他人投稿。

**自动校验清单（免审核的前置条件）**

| 检查项 | 规则 | 失败提示 |
|---|---|---|
| 活动有效性 | 所选活动处于「投稿+评分中」 | 「该活动当前不接受投稿」 |
| 报名状态 | 当前用户在该届已报名 | 「请先报名」 |
| 封面图齐全 | 已选择一张图片 | 「请上传封面图」 |
| 封面类型 | 扩展名 + MIME + 文件头三重判断，三者一致 | 「封面需为 PNG / JPG / WebP」 |
| 封面体积 | ≤ 1 MB | 「封面超过 1 MB，请压缩后再上传」+ 附压缩指引 |
| 外链格式 | 必须是 http/https 绝对 URL；拒绝 `javascript:` / `data:` / 相对路径 / 内嵌账号密码的 URL | 「请填写以 http:// 或 https:// 开头的完整链接」 |
| 外链域名白名单 | 主机名必须命中 `link_domains` 中**已启用**的条目（`exact` 或 `suffix` 匹配）；**不做任何网络探测** | 「暂不支持该托管平台」+ 就地展示已放行平台清单 + 「申请添加托管平台」入口 |
| 提取码 | 该域名被标记为「需密码」时必填，否则可选；1–16 位 | 「该网盘下载需要提取码，请填写」 |
| 重复投稿 | 同届内同一外链视为重复 | 「该链接已被本届其他作品使用」 |
| 频率限制 | 每用户每 10 分钟最多 5 次写操作 | 「操作过于频繁」 |

> **为什么用「域名白名单」而不是「可达性探测」**：国内网盘对服务端自动探测普遍返回 403 或直接拒绝连接，探测结果既不可靠又会误伤合法投稿；反过来，探测通过也证明不了链接长期可用。白名单把判定改成**纯静态、确定性**的一步：主机名在清单里就放行，不在就明确拒绝并给出替代方案。代价是新增托管平台需要管理员先加域名（后台一键操作，见 FR-6.11），换来的是投稿体验的一致性与**零网络依赖**。链接的**长期有效性**不靠投稿时的一次探测，而由每周巡检（见 10.6）+ 详情页失效提示（FR-4.7）+ 用户举报三层事后治理兜底，见 12.8。

### 6.4 作品展示（FR-4）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-4.1 | 活动页作品墙卡片 = 封面图 + 标题 + 作者 | P0 |
| FR-4.2 | 作品详情页展示封面大图、标题、作者、简介，以及**谱面下载外链跳转按钮**（新标签打开，带 `rel="noopener noreferrer nofollow"`）；若作品填了提取码，在按钮旁一并显示并提供一键复制 | P0 |
| FR-4.3 | 封面图统一裁切为固定比例，缺图时显示占位图 | P1 |
| FR-4.4 | 被管理员下架的作品在列表中隐藏（或显示为「已下架」占位） | P0 |
| FR-4.5 | 支持深链分享：作品详情页可直接复制链接分享 | P1 |
| FR-4.6 | 详情页提供 OpenGraph 元信息，分享到聊天软件时能出封面预览 | P2 |
| FR-4.7 | 外链失效时在详情页显示「该链接可能已失效」提示，并提供「报告失效」入口给登录用户 | P1 |

### 6.5 评分与榜单（FR-5）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-5.1 | 登录用户可对作品打 **1–5 星**（整数，半星不做） | P0 |
| FR-5.2 | 一人对一作品只有一条评分记录，重复提交即覆盖（可修改） | P0 |
| FR-5.3 | 评分仅在评分期内可提交/修改；**评分截止（⑤）后完全锁定**，接口拒绝一切增删改 | P0 |
| FR-5.4 | 排行榜按**简单平均分**降序，展示平均分（保留 2 位小数）与评分数；**含作者自评的作品需标注「含作者评分」** | P0 |
| FR-5.5 | 并列时按评分数多者优先，再按投稿时间早者优先 | P1 |
| FR-5.6 | 无评分作品归入「暂无评分」分组 | P1 |
| FR-5.7 | 选手可在「我的评分」页查看与修改自己给出的所有评分 | P1 |
| FR-5.8 | **允许**作者给自己的作品评分；排行榜对该作品标注「含作者评分」（见 17.5） | P1 |
| FR-5.9 | 榜单实时计算（读取时现算），不预存冗余分数字段 | P0 |

### 6.6 管理员后台（FR-6）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-6.1 | **创建 / 编辑活动**：标题、slug、封面、规则说明、**5 个时间戳** | P0 |
| FR-6.2 | **删除作品**（软删除，保留数据可恢复） | P0 |
| FR-6.3 | **下架作品**并填写原因，作品在公开页隐藏但数据库保留 | P0 |
| FR-6.4 | **删除评论 / 隐藏违规评论** | P1 |
| FR-6.5 | **角色管理**：把指定用户设为管理员 / 撤销管理员（不能撤销自己） | P0 |
| FR-6.6 | 报名名单查看（含 GitHub 用户名，便于联系） | P1 |
| FR-6.7 | 导出某届结果 CSV（名次、标题、作者、平均分、评分数） | P1 |
| FR-6.8 | 所有管理动作写入审计日志（谁、何时、对什么、为什么） | P1 |
| FR-6.9 | 后台所有写操作在数据库层校验管理员身份，不依赖前端隐藏按钮 | P0 |
| FR-6.10 | 首个管理员由人工在数据库面板手动设置（引导流程，仅一次） | P0 |
| FR-6.11 | **管理外链域名白名单**（`link_domains`）：增删域名、设置匹配粒度（`exact` / `suffix`）、标记是否需要提取码、启停；变更写入审计日志 | P0 |

### 6.7 评论（FR-7）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-7.1 | 作品详情页提供评论区 | P1 |
| FR-7.2 | 发表评论需登录，评论带 GitHub 头像与用户名 | P1 |
| FR-7.3 | 评论按时间正序展示，支持折叠长楼层 | P2 |
| FR-7.4 | 管理员可删除评论 | P1 |
| FR-7.5 | 评论采用**自建评论表**（`comments`），与作品数据同库，便于统一审核（见 17.7） | P1 |

> 评论承载在自建的 `comments` 表中（见 8.6），不引入 giscus。这样可以与作品数据同库关联、由管理后台统一审核与删除，代价是需要自己做限流与内容治理（见 12.5 同类手段）。

### 6.8 邮件通知（FR-8）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-8.1 | 投稿成功后给作者发确认邮件（含作品链接与可编辑提示） | P1 |
| FR-8.2 | 投稿截止前 24 小时，向「已报名但未投稿」的选手发提醒 | P1 |
| FR-8.3 | 评分截止前 24 小时，向「未评分」的登录用户发提醒（可选，注意退订合规） | P2 |
| FR-8.4 | 结果公布时向全部参赛者发送排名通知 | P1 |
| FR-8.5 | 所有通知可开关（用户档案中的通知偏好） | P2 |
| FR-8.6 | 邮件发送失败不阻塞主流程，失败可重试 | P1 |

> 「截止前提醒」需要定时任务：Supabase 提供 Scheduled Edge Functions / `pg_cron`，Cloudflare 提供 Cron Triggers，具体选型见 10.6。

### 6.9 内容治理与合规（FR-9）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-9.1 | 独立「投稿须知」页：原创性要求、引用与二次创作边界、禁止事项 | P0 |
| FR-9.2 | 投稿时必须勾选**原创与授权声明**（未勾选无法提交） | P0 |
| FR-9.3 | 声明文本与版本号随投稿一并落库（事后可追溯当时的条款） | P1 |
| FR-9.4 | 提供侵权投诉渠道（邮箱）与管理员下架流程（含申诉说明） | P0 |
| FR-9.5 | 隐私声明：说明采集哪些 GitHub 公开信息、存于何处、可否删除 | P0 |
| FR-9.6 | 用户可申请删除自己的账号与投稿数据 | P1 |
| FR-9.7 | 明确的平台免责条款（平台仅提供投稿与展示，版权责任由投稿者承担） | P0 |

### 6.10 明确**不做**的功能（需求已排除）

| 排除项 | 原因 |
|---|---|
| 在线试听 / 音频播放器 | **不做**（见 17.4）：平台不再持有音频文件，无从提供播放器，音乐试听由选手自己的外链承担 |
| 平台代存谱面 / 音乐文件（含「maidata.txt + bg.png/jpg + track.mp3」三文件投稿包） | 改为「封面图 + 公开可访问的谱面外链」，**以减轻在线存储压力**（见 17.1） |
| 服务端自动探测外链可达性 | 改为**域名白名单**静态判定（见 17.12）：探测结果不可靠（网盘普遍返回 403）、会误伤合法投稿，且证明不了长期可用；白名单更确定、零网络依赖 |
| 短链服务（bit.ly 等） | 会绕过域名判断，无法在白名单机制下校验真实目标，需管理员确认后方可加入清单 |
| 关键词搜索、标签筛选、多维排序 | 规模小（单届几十件），卡片墙足够 |
| 内部评委打分环节 | 流程为「公开评分 → 排行榜」，无评审环节 |
| QQ / 微信等国内第三方登录 | 需要企业资质与备案，已放弃 |
| 人工投稿审核队列 | 改为自动校验 + 事后下架 |

---

## 7. 非功能需求

### 7.1 性能（NFR-P）

| 编号 | 指标 | 目标 |
|---|---|---|
| NFR-P.1 | 首屏可交互（LCP） | 桌面 < 2.5 s，移动 4G < 4 s |
| NFR-P.2 | 首包体积（gzip） | < 300 KB |
| NFR-P.3 | 静态资源 | 强缓存 + 内容哈希文件名 |
| NFR-P.4 | 封面图 | 列表页使用缩略图（≤ 150 KB），详情页才加载原图；`loading="lazy"` |
| NFR-P.5 | 作品墙 | 首屏渲染 ≤ 24 张卡，滚动分页加载 |
| NFR-P.6 | 榜单查询 | 单届 ≤ 100 件作品时 < 500 ms |

### 7.2 兼容性与响应式（NFR-C）

| 编号 | 要求 |
|---|---|
| NFR-C.1 | 移动端与桌面端**并重**，断点至少覆盖 375 / 768 / 1280 / 1920 |
| NFR-C.2 | 移动端作品墙 1–2 列，桌面 3–4 列 |
| NFR-C.3 | 触控目标 ≥ 44×44 px |
| NFR-C.4 | 支持最新两个大版本的 Chrome / Edge / Firefox / Safari（含 iOS Safari） |
| NFR-C.5 | 移动端上传封面支持直接调用相册/相机 |

### 7.3 视觉（NFR-V）

| 编号 | 要求 |
|---|---|
| NFR-V.1 | **深色音游风**：深底 + 霓虹/渐变点缀（强调色高饱和、可用辉光与细描边） |
| NFR-V.2 | 支持**深浅色切换**，默认跟随系统 `prefers-color-scheme`，用户选择持久化 |
| NFR-V.3 | 颜色全部走 CSS 变量，切换主题不重绘组件树 |
| NFR-V.4 | 正文对比度 ≥ 4.5:1（深色模式下尤其注意霓虹色当正文用） |
| NFR-V.5 | 动效克制：仅用于状态反馈与悬停，尊重 `prefers-reduced-motion` |

### 7.4 可用性与可访问性（NFR-A）

| 编号 | 要求 |
|---|---|
| NFR-A.1 | 全站键盘可达，焦点样式清晰 |
| NFR-A.2 | 覆盖 loading / empty / error / 无权限 四类状态，均有明确文案与下一步动作 |
| NFR-A.3 | 表单校验错误就近提示，并说明如何修正 |
| NFR-A.4 | 所有图片有 `alt`，装饰图 `alt=""` |
| NFR-A.5 | 中文文案统一、口语化，避免技术黑话暴露给选手 |

### 7.5 安全（NFR-S）

| 编号 | 要求 |
|---|---|
| NFR-S.1 | **所有权限判定在数据库行级安全策略执行**，前端仅做体验优化 |
| NFR-S.2 | 前端只使用可公开的匿名密钥；任何服务端密钥**绝不进入前端产物** |
| NFR-S.3 | 评分唯一性由数据库唯一约束保证，而非前端判断 |
| NFR-S.4 | 文件上传路径按「活动/用户」隔离，禁止任意路径写入 |
| NFR-S.5 | 文件类型既校验 MIME 也校验文件头，图片需可解码 |
| NFR-S.6 | 用户输入渲染为**纯文本或净化后的 Markdown**，杜绝 HTML 注入 |
| NFR-S.7 | 外部链接使用 `rel="noopener noreferrer nofollow"` |
| NFR-S.8 | 关键写操作频率限制，防脚本刷榜 |
| NFR-S.9 | 管理动作全部审计留痕 |
| NFR-S.10 | 定期自查 RLS：用「匿名 / 普通用户 / 管理员」三种身份分别验证越权读取与写入是否被拒 |

### 7.6 SEO 与可分享（NFR-E）

| 编号 | 要求 |
|---|---|
| NFR-E.1 | 每个路由有独立标题与 description |
| NFR-E.2 | `sitemap.xml` + `robots.txt` |
| NFR-E.3 | 活动页与作品详情页提供 OpenGraph / Twitter Card |
| NFR-E.4 | 说明：纯 SPA 对搜索引擎不友好，折中方案见 10.7 |

### 7.7 运维与可维护性（NFR-M）

| 编号 | 要求 |
|---|---|
| NFR-M.1 | 推送即自动构建部署（Cloudflare Pages 连接 GitHub 仓库） |
| NFR-M.2 | 环境变量区分开发 / 生产，生产密钥只在托管平台配置 |
| NFR-M.3 | 数据库结构变更用**迁移脚本**管理，禁止手工改线上表 |
| NFR-M.4 | 免费额度用量监控（请求数、存储、出网、邮件发送量）与阈值告警 |
| NFR-M.5 | 数据可导出（管理后台 CSV + 数据库面板备份），不依赖单一平台 |
| NFR-M.6 | 关键流程（登录、投稿、评分）有端到端健康检查手段 |

---

## 8. 数据模型（概念设计，非 SQL）

> 以下为表级概念描述，用于评审业务语义。字段名为概念名，落地时再定具体命名。

### 8.1 `profiles`（用户档案）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键，与认证服务的用户 ID 一致 |
| github_login | 文本 | GitHub 用户名，唯一 |
| display_name | 文本 | 展示名，可改 |
| avatar_url | 文本 | GitHub 头像地址 |
| role | 枚举 | `user` / `admin`，默认 `user` |
| notify_email | 布尔 | 邮件通知开关 |
| created_at | 时间戳 | 首次登录时间 |

> 角色放在本表而非认证服务的元数据里，便于后台用一次数据库更新完成授权，且可被 RLS 直接引用。

### 8.2 `events`（活动 / 届次）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| slug | 文本 | URL 友好唯一标识，如 `2026-spring` |
| title | 文本 | 活动标题 |
| summary | 文本 | 一句话简介（主页卡片用） |
| cover_url | 文本 | 活动封面 |
| rules_md | 文本 | 活动规则（Markdown） |
| signup_opens_at | 时间戳 | ① 报名开始 |
| submit_opens_at | 时间戳 | ② 投稿开始 |
| signup_closes_at | 时间戳 | ③ 报名结束 |
| submit_closes_at | 时间戳 | ④ 投稿截止 |
| rating_closes_at | 时间戳 | ⑤ 评分截止 |
| created_by | UUID | 创建的管理员 |
| created_at | 时间戳 | — |

**约束**：五个时间戳必须满足 ① ≤ ② ≤ ③ ≤ ④ ≤ ⑤。

### 8.3 `registrations`（报名）

| 字段 | 说明 |
|---|---|
| event_id | 活动 ID |
| user_id | 用户 ID |
| created_at | 报名时间 |

**唯一约束**：`(event_id, user_id)` —— 一人一届只能报名一次。

### 8.4 `works`（作品）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| event_id | UUID | 所属活动 |
| user_id | UUID | 投稿者 |
| title | 文本 | 作品标题 |
| author_display | 文本 | 卡片上显示的作者名（快照，避免改档案影响历史） |
| cover_path | 文本 | 封面图存储路径（**平台唯一需要承载的投稿文件**） |
| cover_bytes | 数值 | 封面体积，用于额度统计与超限提示 |
| chart_link | 文本 | 谱面下载外链（公开可访问的 http/https URL，主机名必命中白名单） |
| chart_link_host | 文本 | 外链主机名（冗余存储，便于按托管平台统计与巡检分批） |
| chart_link_password | 文本 | 提取码 / 分享密码，仅当白名单标记该域名「需密码」时写入；可空 |
| chart_link_status | 枚举 | `ok` / `unreachable` / `unchecked`，**仅由每周定时巡检维护**（投稿时不做任何网络探测） |
| chart_link_checked_at | 时间戳 | 最近一次巡检时间 |
| description | 文本 | 简介 |
| status | 枚举 | `published` / `hidden` / `removed` |
| removed_reason | 文本 | 下架原因 |
| terms_version | 文本 | 投稿时接受的声明版本 |
| created_at / updated_at | 时间戳 | 投稿时间用于并列排名 |

**索引**：`(event_id, status, created_at)` 覆盖作品墙查询；**在本届范围内对 `chart_link` 建唯一约束**，支撑 6.3 的同届外链查重；`chart_link_host` 建普通索引，供巡检与托管平台统计分批。

### 8.5 `ratings`（评分）

| 字段 | 说明 |
|---|---|
| id | UUID |
| event_id | 冗余存一届 ID，便于按届聚合与约束 |
| work_id | 被评作品 |
| user_id | 评分者 |
| score | 整数 1–5 |
| created_at / updated_at | 修改时间 |

**唯一约束**：`(work_id, user_id)` —— 一人一作品一条评分（修改 = 覆盖）。
**校验**：`score BETWEEN 1 AND 5`。
**榜单查询**：按 `event_id` 分组，对 `score` 求平均与计数，按平均降序。

### 8.6 `comments`（评论）

| 字段 | 说明 |
|---|---|
| id / work_id / user_id | 主键、所属作品、评论者 |
| body | 评论正文（限长，纯文本） |
| hidden | 是否被管理员隐藏 |
| created_at | 时间 |

### 8.7 `notifications`（通知记录）

| 字段 | 说明 |
|---|---|
| id / user_id | — |
| type | `submit_ok` / `deadline_reminder` / `result_published` |
| event_id / work_id | 关联对象 |
| status | `pending` / `sent` / `failed` |
| provider_message_id | 邮件服务返回的消息 ID |
| created_at / sent_at | — |

**用途**：去重（同一提醒只发一次）+ 失败重试 + 排障。

### 8.8 `audit_logs`（审计日志）

| 字段 | 说明 |
|---|---|
| id / actor_id | 操作者 |
| action | `create_event` / `delete_work` / `hide_work` / `grant_admin` / `revoke_admin` … |
| target_type / target_id | 操作对象 |
| reason | 原因（删除/下架必填） |
| created_at | 时间 |

### 8.9 `link_domains`（外链域名白名单）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| pattern | 文本 | 域名或主机名匹配串，如 `pan.baidu.com`、`github.com`、`lanzou.com` |
| match_type | 枚举 | `exact`（精确主机名）/ `suffix`（该域及其子域一并放行） |
| needs_password | 布尔 | 该域名下的分享页是否需要提取码；为真时投稿表单的提取码字段变为必填 |
| label | 文本 | 展示用名称（如「百度网盘」），用于「已放行平台清单」页面 |
| enabled | 布尔 | 停用后不再放行新投稿，**历史作品不受影响** |
| sort_order | 数值 | 清单展示顺序，可按推荐度排序 |
| created_by / created_at | — | 谁在何时加入，便于回溯与审计 |

**种子清单**（首次部署由迁移脚本写入，之后全部由管理员在后台维护，见 FR-6.11）

| 类别 | 域名 |
|---|---|
| 代码托管 / 发布 | `github.com`（含 Release 页）、`raw.githubusercontent.com`、`objects.githubusercontent.com`、`gitee.com`、`gitlab.com`、`jsdelivr.net` |
| 对象存储直链 | `*.r2.dev`、R2 / S3 兼容自定义域、阿里云 OSS 与腾讯云 COS 的公开读直链（由管理员按实际域名补录） |
| 国内网盘（需提取码） | `pan.baidu.com`、`lanzou.com` 及其常用备用域、`123pan.com`、`alipan.com`、`cloud.189.cn`、`pan.quark.cn` |
| 海外网盘 | `1drv.ms`（OneDrive）、`drive.google.com`、`mega.nz`、`dropbox.com` |

> **匹配规则**：一律以**解析后的最终主机名**比对（先 `UPPER → lower`、去掉末尾点、必要时做 punycode 归一），`suffix` 类型要求主机名**等于** pattern 或以 `.pattern` 结尾。这样可避免 `evil-pan.baidu.com.attacker.com` 这类后缀伪造命中——它既不等于 `pan.baidu.com`，也不以 `.pan.baidu.com` 结尾，会被正确拒绝。

### 8.10 实体关系概览

```
profiles ──1:N── works ──N:1── events
    │             │              │
    │             └──1:N── comments
    │             └──1:N── ratings ──N:1── profiles
    └──N:M(registrations)── events
    └──1:N── notifications
    └──1:N── audit_logs
```

`link_domains` 是**独立表**（无外键），只在投稿校验时被查询、在后台被维护——works 表通过冗余的 `chart_link_host` 与它对上，不做外键约束，这样停用某个域名不会影响历史作品。

---

## 9. 技术栈选型

### 9.1 总览

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | **Vue 3 + Vite + TypeScript** | 生态成熟、构建快、类型安全；需求已确认 |
| 状态管理 | **Pinia** | Vue 官方推荐，轻量，适合会话与活动状态 |
| 路由 | **Vue Router** | 官方方案，支持守卫做登录拦截 |
| 样式 | **自写样式（CSS 变量 + 轻量工具类）** | 深色音游风需要高度自定义视觉，组件库主题往往拖后腿 |
| UI 组件 | **按需引入**（如需要的弹窗、日期选择器） | 避免整包引入拖大首包 |
| 后端 | **Supabase** | 一站式提供 GitHub OAuth + Postgres + 行级安全 + 文件存储，省掉自建 OAuth 中转层 |
| 数据库 | **Postgres（Supabase 托管）** | 关系模型贴合活动/作品/评分，约束与聚合查询天然合适 |
| 鉴权 | **Supabase Auth 的 GitHub Provider** | 不开设置密码流程，登录即 GitHub，与 giscus 体验一致 |
| 文件存储 | **Supabase Storage** | 只存**投稿封面图**与**活动封面**（体积极小）；谱面文件由选手自备外链，**平台不承载** |
| 静态托管 | **Cloudflare Pages** | 静态资源带宽不限量、全球 CDN、免费额度宽松；构建与部署对接 GitHub |
| 邮件 | **Resend**（备选 Brevo） | 免费 3,000 封/月、无强制品牌页脚、API 简洁 |
| 评论 | **Supabase 自建评论表**（`comments`） | 与作品数据同库，管理后台可统一审核与删除 |
| 定时任务 | **Supabase Scheduled Edge Functions**（或 `pg_cron`） | 截止提醒与结果通知 |
| 图表 | 暂不需要 | 榜单用表格即可 |

### 9.2 为什么放弃「自建 GitHub OAuth 中转层」

最初方案是模仿 giscus：用 Cloudflare Workers / 边缘函数做 OAuth 中转，加密 `state`、校验 `Origin`、把 token 换回前端。**该方案已废弃**，原因：

1. Supabase Auth 内置 GitHub Provider，**开箱即用**，无需自己实现 OAuth 握手与 state 加密；
2. 自建中转层要自己处理：密钥保管、CSRF、开放重定向、token 刷新、多域白名单 —— 每一条都是安全陷阱；
3. Supabase 的会话是**服务端签发的 JWT**，数据库 RLS 可以直接用它判定身份，而自建方案还要把身份再传给数据库，链路更长；
4. 少一套边缘服务就少一处故障点与一份免费额度消耗。

> 结论：giscus 的「范式」（GitHub OAuth + 无需密码 + 与 GitHub 数据源联动）值得借鉴，但**具体实现**交给 Supabase 这类 BaaS 更稳妥。

### 9.3 关键技术选型对比（备选方案）

| 决策点 | 选定 | 备选 1 | 备选 2 | 为什么不选备选 |
|---|---|---|---|---|
| 后端形态 | Supabase（Postgres + Auth + Storage） | Firebase（NoSQL + Auth + Storage） | 纯静态 + GitHub API 提交 | Firebase 是文档模型，做平均分排行与唯一约束不自然，且迁移成本高；「仓库即数据库」在并发评分下必然冲突 |
| 数据存储 | Postgres + RLS | Cloudflare D1 | PocketBase（自托管） | D1 生态较新、边缘 SQL 限制多；PocketBase 需自运维，违背「零运维」原则 |
| 静态托管 | Cloudflare Pages | Vercel（Hobby） | GitHub Pages | Vercel Hobby 免费档定位为非商业个人用途，团队赛事使用有合规风险；GitHub Pages 无法设置 HTTP 响应头（影响缓存与安全头） |
| 文件存储 | Supabase Storage | Cloudflare R2 | 外部图床 | R2 出网免费但需写 Worker 做鉴权，复杂度上升；外部图床无法做权限控制与合规下架 |

---

## 10. 部署与运维

### 10.1 部署拓扑

```
GitHub 仓库 (FoliAge-0x76/hdssibal)
        │  git push
        ▼
Cloudflare Pages ──自动构建──▶ 静态站点（全球 CDN）
        │
        │ 浏览器直接调用（只带公开的匿名密钥）
        ▼
Supabase
  ├─ Auth      GitHub OAuth 登录
  ├─ Postgres  活动 / 报名 / 作品 / 评分 / 评论 / 通知 / 审计
  ├─ Storage   投稿封面图 + 活动封面（谱面走外链，不进 Storage）
  └─ Edge Functions  定时任务（截止提醒、结果通知）
        │
        ▼
Resend  事务性邮件
```

关键点：**浏览器不经过任何自建中间层直连 Supabase**，所有权限由 RLS 执行。

### 10.2 GitHub OAuth 配置（需人工在 GitHub 侧操作）

| 步骤 | 操作 | 位置 |
|---|---|---|
| 1 | 新建 OAuth App | GitHub → Settings → Developer settings → OAuth Apps |
| 2 | Homepage URL 填站点地址（Cloudflare Pages 分配的域名） | — |
| 3 | **Authorization callback URL** 填 Supabase 给出的回调地址（形如 `https://<project-ref>.supabase.co/auth/v1/callback`） | 必须与 Supabase 面板显示的完全一致 |
| 4 | 抄下 Client ID / 生成 Client Secret | 填入 Supabase Auth 的 GitHub Provider 设置 |
| 5 | 在 Supabase 允许的重定向地址白名单里加入站点域名（含本地开发地址） | Supabase → Authentication → URL Configuration |

> 注意：Cloudflare Pages 每次部署会生成预览域名，若希望在预览环境登录，需把预览域名也加入白名单。

### 10.3 部署流水线（需人工一次性配置）

1. Cloudflare Dashboard → Pages → Connect to Git → 选择 `hdssibal` 仓库。
2. 构建命令用 `npm run build`，输出目录 `dist`，Node 版本按项目要求指定。
3. 在 Pages 的 Environment Variables 里配置前端可公开的 Supabase 地址与匿名密钥（**不加 `VITE_` 前缀就无法被构建注入**，反之要注意不要把服务端密钥加上 `VITE_` 前缀）。
4. 绑定自定义域名（首发**不绑**，见 12.1 与 17.11）。
5. 生产分支设为 `main`，PR 自动生成预览部署。

### 10.4 首个管理员引导

1. 用你的 GitHub 账号登录一次站点，让 `profiles` 里出现你的记录。
2. 在 Supabase 表编辑器里把该行 `role` 改为 `admin`。
3. 之后所有授权都在站点后台完成，不再需要手改数据库。

### 10.5 备份与恢复

- 免费档 Supabase 不提供自动备份，需**定期手动导出**（数据库结构 + 数据 + Storage 文件清单）。
- 建议节奏：每届活动结束后立即导出一次；平时每周一次。
- 导出物存到 GitHub 私有仓库或本地，**不要含任何密钥**。

### 10.6 定时任务方案

| 任务 | 触发时间 | 实现 |
|---|---|---|
| 投稿截止前提醒 | 截止前 24 h | 定时函数查询「已报名未投稿」名单 → 调 Resend |
| **谱面外链巡检** | 每周一次 | 按 `chart_link_host` 分批探测在售作品的 `chart_link`，更新 `chart_link_status` 与 `chart_link_checked_at`；转为失效则邮件提醒作者（见 12.8）。这是**全站唯一会对外链发起请求的地方**——投稿流程本身零网络依赖 |
| 结果公布 | 评分截止后由管理员手动触发 | 后台按钮 → 定时函数 → 批量发送 |
| 项目保活 | 每 3–5 天 | 见 12.2 |

### 10.7 SPA 的 SEO 折中

纯 SPA 的 HTML 是空壳，搜索引擎抓到的内容有限。按成本从低到高：

1. **只做基础**：动态 `title` / `description` / OpenGraph，主要靠分享而非搜索引流（推荐，成本最低）；
2. **预渲染**：用构建期预渲染为公开页（主页、活动页、作品详情）产出静态 HTML，交互页仍走 SPA；
3. **改 SSR 框架**：迁移到 Nuxt 等，需要后端运行时，超出免费静态托管范畴。

> 考虑到规模是校内赛、流量主要来自社群分享，建议先做方案 1，观察是否需要升级。

---

## 11. 免费额度与成本测算

### 11.1 Supabase 免费档（2026-09-15 核实）

| 项目 | 额度 |
|---|---|
| 活跃项目数 | 2 个 |
| 数据库容量 | 500 MB |
| 文件存储 | 1 GB |
| 出网流量 | 5 GB + 5 GB 缓存 |
| 月活用户（MAU） | 50,000 / 组织 |
| Edge Functions | 500,000 次/月 |
| 单文件上限 | 50 MB |
| 日志保留 | 1 天 |
| ⚠️ 休眠规则 | **7 天无活动自动暂停**，需登录面板手动恢复 |

### 11.2 Cloudflare Pages 免费档（2026-09-15 核实）

| 项目 | 额度 |
|---|---|
| 每月构建次数 | 500 次 |
| 并发构建 | 1 |
| 单次构建超时 | 20 分钟 |
| 单文件大小 | 25 MiB |
| 每站文件数 | 20,000 |
| 自定义域名 | 100 个 |
| 静态资源带宽 | **不计费、不限量** |
| Pages Functions | 100,000 请求/天，单次 10 ms CPU |

### 11.3 邮件服务免费档对比（2026-09-15 核实）

| 服务 | 免费额度 | 备注 |
|---|---|---|
| **Resend** | 3,000 封/月（约 100/天） | 无强制品牌页脚、免信用卡 ← 首选 |
| Brevo | 300 封/天 | 邮件带品牌页脚 |
| Mailgun | 100 封/天 | 需信用卡，日志仅保留 1 天 |
| SendGrid | 仅 60 天试用 | 免费长期档已取消 |

### 11.4 本项目的额度测算

假设单届：**60 件作品、400 名登录用户、每人评 10 件**。

| 资源 | 测算 | 占免费额度 |
|---|---|---|
| 数据库 | 60 作品 + 4,000 评分 + 400 档案 + 少量日志 ≈ **< 20 MB** | 4% |
| 存储 | 60 张封面 ≤ 1 MB ≈ **60 MB**（谱面走外链，**0 占用**） | 6% |
| 出网 | 静态资源走 Cloudflare（不计费）；数据库读 + Storage 读 ≈ 数 GB | 需留意 |
| 邮件 | 投稿确认 60 + 提醒 100 + 结果 60 ≈ **220 封** | 7% |
| 构建 | 一次活动周期内代码推送约 20–50 次 | 10% 以内 |

**结论**：**Storage 不再是瓶颈**。改为「封面图 + 谱面外链」后，平台侧存储从 660 MB 降到 **60 MB（6%）**，单届额度余量从 34% 提升到 **94%**，可以连续支撑十几届活动而不必清理历史。

代价是**风险发生了转移**：从「额度不够」变成了「外链外部依赖」——选手自备的链接可能被删、被改权限、被限速，平台对此没有控制力。这一风险先在入口处用**域名白名单**（见 17.12）收窄（排除短链、临时托管与未知站点），再由**每周巡检 + 详情页失效提示 + 用户举报**三层事后治理兜底，详见 **12.8**。

### 11.5 成本结论

按当前规模，**全流程可 0 元运行**。可能的付费触发点：

| 触发条件 | 建议动作 |
|---|---|
| 作品量增长导致 Storage 超 1 GB | 收紧封面体积上限 / 迁移往届封面 / 升级 Supabase Pro（当前测算仅用 6%，短期内不会触及） |
| 需要自动备份 | 手动导出脚本 + 本地存储（0 成本） |
| 邮件量超 3,000/月 | 换 Brevo 或自建 SMTP |
| 需要更稳定的国内访问 | 见 12.1 的域名与 CDN 方案 |

---

## 12. 风险与缓解

### 12.1 🔴 中国大陆访问可用性（**最高风险**）

**问题**：已核实的实测与社区反馈显示：

- Cloudflare Pages 的 `pages.dev` 默认域名在中国大陆**并非全国性硬封锁，但属于「间歇性可访问」**；
- 部分地区与运营商（如河北、江苏）出现 `ERR_CONNECTION_TIMED_OUT`，使用代理才能正常访问；
- 存在 **DNS 污染、路由丢包、连接重置（RST）** 等干扰，不同运营商策略差异大；
- 延迟普遍在 **180–600 ms**，高峰时段可能超时；
- 长期趋势不乐观，**不推荐直接用 `pages.dev` 作为面向大陆用户的生产域名**。

**叠加风险**：Supabase 官方云**没有中国大陆节点**，最近的新加坡节点 REST 延迟常在 700 ms 以上，首包 1–2 s，且有丢包重传。这意味着**即使静态站能打开，数据加载仍可能明显偏慢**。

**缓解方案（按成本排序）**

| 方案 | 成本 | 效果 |
|---|---|---|
| **A. 绑定自有域名**，并尽量走 Cloudflare 代理 | 域名年费 | 摆脱 `pages.dev` 被批量拦截的风险，可灵活调整解析 |
| **B. 国内 / 海外智能 DNS 分流**，国内解析到国内 CDN | CDN 费用 | 大陆访问速度显著改善 |
| **C. 静态站双部署**：Cloudflare Pages（海外）+ 国内对象存储/CDN（大陆），按访客 IP 分流 | 国内 CDN 费用 | 静态部分体验最好，但后端仍在海外 |
| **D. 保守方案**：只在社群内分发，接受「大陆偶发不可访问」 | 0 | 参赛者可能无法访问，**不建议作为长期方案** |
| **E. 换国内云**：静态资源上阿里云 OSS + CDN / 腾讯云 COS + CDN，后端自建或换国内 BaaS | 需要备案 | 体验最好，但违背「零运维」原则，且有数据出境合规要求 |

**务实建议**（结合 17.11 的决策）：首发按你的决定**先不绑域名**，即暂时落在 **D** 这一档；把「大陆访问偶发失败」写进选手须知并给出备用入口（如临时分享的作品压缩包），活动期间观察实际失败率。一旦确认要长期运营，**A 是第一步且成本极低**（仅域名年费），失败率高时再上 **B** 或 **C**。

> 补充：若最终希望纯静态、零后端并追求更稳的大陆访问，可评估**迁回 GitHub Pages + 国内 CDN 加速**，但 GitHub Pages 无法自定义 HTTP 响应头，且同样依赖国际出口，收益有限。

### 12.2 Supabase 免费项目 7 天休眠

**问题**：免费项目连续 7 天无活动会被**自动暂停**，需登录面板手动恢复。若选在两届活动之间的空档期休眠，恢复时可能影响选手访问。

**缓解**：

- 用 Cloudflare Cron Triggers 或外部免费监控（如 UptimeRobot）**每 3–5 天发一次轻量请求**保活；
- 在后台管理页显示「项目保活状态」；
- 或接受休眠，在活动预告页注明「活动开始前会恢复服务」。

### 12.3 文件大小上限（已大幅简化）

改为「封面图 + 谱面外链」后，**平台侧只剩一个上传入口**：

| 文件 | 业务上限 | Supabase Storage 硬上限 | 风险 |
|---|---|---|---|
| 投稿封面图 | **1 MB** | 50 MB | 低。1 MB 对未压缩的高质量 PNG 偏紧，需配**可操作的压缩指引** |
| 活动封面 | 2 MB | 50 MB | 管理员维护，数量极少 |

**谱面文件不再经过平台**，因此没有平台侧体积限制——它的大小与格式完全由选手自备的外链决定，相应风险见 12.8。

另有 Cloudflare Pages 的**单静态文件 25 MiB** 限制，但它只作用于随代码发布的资源，**不影响用户上传**，与本节不冲突。

**缓解**：前端选中封面后立刻读 `size` 并拦截，超限时给出**可操作的压缩指引**（推荐转 WebP、质量 80，通常可压到 300 KB 以内，同时附一个在线压缩工具的链接）；后端再校验一次，不留绕过空间。1 MB 是本次决策的下限档（见 17.16），若实际投稿中压缩门槛过高，可随时在后台把上限调回 2 MB——数据库测算是 60 MB，留有很大的调整余地。

### 12.4 RLS 配置错误导致数据泄漏 / 越权写入

**问题**：这是此类架构最典型的事故。写成「允许所有已登录用户更新所有行」会让人改掉别人的作品。

**缓解**：

- 每条策略都明确 `USING`（可读哪些行）与 `WITH CHECK`（可写成什么样）两侧；
- 角色判定统一走 `profiles.role` 的子查询，不信任前端传来的任何角色字段；
- 建立**三身份回归测试清单**（匿名 / 普通用户 / 管理员），每次改策略后逐条手工验证；
- 禁止在生产环境直接改策略，走迁移脚本 + 本地验证。

### 12.5 评分被刷

**缓解**：一人一作品一条评分唯一约束；写操作频率限制；记录评分的创建/修改时间以便事后审查；管理员可导出异常评分（如同一时间窗内大量同分值）用于人工判断。

### 12.6 版权与合规

**缓解**：投稿须知 + 强制勾选原创/授权声明 + 声明版本随投稿落库 + 侵权投诉邮箱 + 管理员快速下架能力。平台在显著位置声明「版权责任由投稿者承担」。

### 12.7 单一平台依赖

**缓解**：数据可导出（CSV + 数据库面板）；Supabase 为开源产品**可自托管**，迁移路径存在；前端是标准静态站，更换托管平台成本低。

### 12.8 谱面外链失效 / 指向恶意内容

这是「封面图 + 外链」方案**为换取存储空间而引入的代价**。引入**域名白名单**（见 17.12）后，风险等级从**中高降到中低**：白名单把未知站点、短链、临时文件托管挡在门外，剩下的问题集中在**链接失效**。

| 子风险 | 表现 | 缓解 |
|---|---|---|
| **链接失效** | 网盘文件被删、分享链接过期、提取码被改、GitHub Release 被移除、作者转移仓库 | ① 每周定时巡检并邮件提醒作者（10.6）② 详情页「可能已失效」提示（FR-4.7）③ 登录用户可「报告失效」④ 管理员可下架 |
| **链接被限速 / 需要登录** | 网盘分享页要求登录才能下载，或限速到无法使用 | 详情页明确标注「本链接指向第三方站点，可能需要登录或存在限速」；投稿页提供「推荐用 GitHub Release 直链」的指引 |
| **链接指向恶意 / 侵权内容** | 外链指向挂马站点、盗版资源 | **白名单已消除大部分可能**：只有已知托管平台能通过校验。剩余手段：① 跳转带 `rel="noopener noreferrer nofollow"` 且 `target="_blank"` ② 登录用户可举报 ③ 管理员可下架（FR-6.3） |
| **白名单被滥用** | 选手借 GitHub Release 上传侵权内容 | 投稿时的原创声明 + 举报入口 + 管理员下架；**域名白名单只保证「域名可控」，不承担内容审核职责** |

**为什么不做可达性探测**：探测结果不可靠（网盘对服务端请求普遍返回 403）、有网络延迟与超时成本，且通过一次探测也证明不了长期可用。因此分工是——**入口处用静态白名单保证「平台可识别」，事后用巡检保证「链接还活着」**。

**额外一条设计约束**：**平台不得代理下载或转存外链内容**——一旦代下载，平台就重新变成了内容分发方，存储压力与版权责任会一并回来，与本方案的初衷相悖。

**白名单的运营职责**：管理员需定期复核实际使用频率高的托管平台，把出现大量违规内容的域名停用（`enabled = false`，历史作品不受影响），并在发现新的稳定托管平台时及时补录，避免选手因「平台不认识」而无法投稿。

---

## 13. 分期里程碑

| 阶段 | 目标 | 交付内容 | 验收标准 |
|---|---|---|---|
| **M0 基建** | 打通链路 | 项目脚手架、Cloudflare Pages 部署、Supabase 项目与 GitHub OAuth、首个管理员 | 能在线上打开空站点并用 GitHub 登录 |
| **M1 数据与权限** | 打好地基 | 表结构（含 `link_domains` 白名单与种子清单）+ RLS 策略 + 三身份回归清单 | 匿名/用户/管理员分别验证读写边界全部正确 |
| **M2 活动与展示** | 能看 | 主页最近活动、活动列表、活动页与时间轴、作品墙骨架（先用假数据） | 主页能显示最近活动；活动页能显示卡片墙 |
| **M3 投稿闭环** | 能投 | 报名、投稿表单（封面图 + 谱面外链 + 提取码 + 声明）、白名单校验与自动校验、我的作品、编辑与删除 | 选手能完整走完报名→投稿→看到自己的作品；非白名单域名被正确拒绝并给出引导 |
| **M4 评分与榜单** | 能评 | 评分控件、评分唯一约束、排行榜、我的评分 | 多人评分后榜单平均值正确，重复评分被覆盖 |
| **M5 管理后台** | 能管 | 建活动、删/下架作品、角色授予、**外链域名白名单维护**、报名名单、结果导出、审计日志 | 管理员能完整运营一届活动；新增托管平台后无需改代码即可放行 |
| **M6 增强** | 好用 | 邮件通知（3 类）、定时任务、评论、往届归档页 | 三类邮件均能收到；往届可翻查 |
| **M7 打磨** | 上线 | 视觉（深色音游风 + 主题切换）、响应式、可访问性、空/错状态、SEO 元信息、性能优化 | 移动端与桌面端体验一致；LCP 达标 |
| **M8 试运行** | 验证 | 用一届「测试活动」全流程压测（模拟 60 件作品 / 400 用户） | 无阻断性缺陷，额度未触顶 |

> 建议按 M0→M4 先打通「报名→投稿→评分→榜单」的**最小可用闭环**，再回头做后台与增强项，这样可以在早期就发现权限与数据模型的问题。

---

## 14. 如何检索「可用于部署本网站的工具」（方法论）

> 这是你特别要求的章节。核心难点在于：**如何在没有专门信息源的情况下，快速找到贴合本项目特点的免费工具，而不是误用看似相近但范式不同的工具。**

### 14.1 先诊断：本项目到底缺哪些「能力」，而不是缺哪些「软件」

不要先搜工具，先列**能力缺口清单**。本项目需要：

| # | 能力缺口 | 说明 |
|---|---|---|
| 1 | 静态前端托管 | 能构建 Vue/Vite 产物、全球分发、支持自定义域名与 HTTPS |
| 2 | 用户身份与第三方登录 | 尤其是 **GitHub OAuth** |
| 3 | 关系型数据存储 | 需要唯一约束、聚合（求平均）、事务 |
| 4 | **行级/行内权限控制** | 让「只能改自己的作品」由数据库执行，而非前端 |
| 5 | 文件存储（图片与压缩包） | 带访问权限、可下架 |
| 6 | 事务性邮件 | 投稿确认、截止提醒、结果公布 |
| 7 | 定时任务 | 定时触发提醒与结算 |
| 8 | 评论承载 | 作品详情页的讨论区 |
| 9 | 构建与部署流水线 | 推送即部署 |
| 10 | 监控与用量告警 | 免费额度的使用情况 |

**用这个清单当检索引擎的输入**，而不是用「比赛平台」这类业务词去搜 —— 业务词搜不到通用工具。

### 14.2 为什么 MkDocs / Docusaurus / VitePress 这类工具不适合本项目

这是最典型的「范式不匹配」陷阱。它们属于**文档站点生成器（Static Site Generator / Docs Generator）**，范式是：

> 一堆 Markdown → 构建 → 只读静态 HTML

它们**天生没有**：登录、写入、数据库、文件上传、权限判定、事务 —— 所以：

- 用户无法「登录」，只能靠外挂评论系统（giscus）提供身份；
- 用户无法「上传作品」，因为没有任何可变状态可以落盘；
- 「排行榜」无法动态计算，因为它只是构建期生成的静态页。

**本项目的范式是「SPA / Jamstack + 后端服务（BaaS）」**：前端是应用（有登录态、有表单、有写操作），后端是有状态服务。所以检索方向应该是**静态托管 + 后端即服务（BaaS）**，而不是文档生成器。

> 一句话判据：**页面内容是否因用户操作而改变？** 会 → 你需要 BaaS；不会 → 文档生成器就够。

### 14.3 检索渠道清单

| 类型 | 渠道 | 适用场景 |
|---|---|---|
| 免费额度聚合 | [free-for.dev](https://free-for.dev/) | 一站式查「某服务免费档给多少」，最省事 |
| 平台官方定价页 | 各服务官网 Pricing / Free Tier 页 | **唯一权威**来源，媒体文章常过时 |
| 官方「替代方案」页 | 如 Supabase 的 Alternatives、Auth0 的 Alternatives | 顺着竞品关系网横向扩散 |
| GitHub Topics | `topic:jamstack`、`topic:baas`、`topic:comment-system`、`topic:oauth2`、`topic:static-site-generator` | 找开源实现与严肃项目 |
| Awesome 清单 | `awesome-selfhosted`、`awesome-vue`、`awesome-jamstack`、`awesome-baas`、`awesome-selfhosted-*` | 有人替你做过初筛，但需自己验证活跃度 |
| 静态站方向专门目录 | [StaticGen](https://www.staticgen.com/)、[Jamstack 官网](https://jamstack.org/) | 定位「静态生成器 / 托管」赛道 |
| 产品发现 | Product Hunt、Hacker News（Algolia 搜索历史帖） | 找新服务与真实评价 |
| 对比/替代站 | AlternativeTo、Slant | 找同赛道候选 |
| 开发者社区 | Reddit（r/webdev、r/selfhosted）、V2EX、掘金 | 找**真实踩坑与国内访问体验** |
| 中文检索（大陆可用性） | 知乎、掘金、少数派，搜「XX 国内 访问」「XX 被墙」 | 前面那些渠道**不会**告诉你大陆能不能访问 |
| 状态页 | 各服务 status 页、第三方监控站 | 判断稳定性 |

### 14.4 可直接复制使用的检索词

按能力缺口逐个搜，不要一次搜「比赛平台工具」：

```
静态托管
  free static site hosting custom headers
  static hosting with functions free tier
  "cloudflare pages" vs "vercel" vs "netlify" free tier comparison

后端即服务（核心方向）
  backend as a service for static site
  BaaS with row level security
  supabase alternatives free tier
  postgres free tier with auth and storage
  edge database free tier

鉴权
  github oauth provider as a service
  oauth for static site without backend
  supabase auth github provider setup
  self-hosted oauth server lightweight

文件存储
  object storage free tier no egress fee
  image upload service for static site
  file storage with access control free tier

评论
  comment system for static site
  giscus alternatives
  self-hosted comment system lightweight
  github discussions based comments

邮件
  transactional email free tier 3000
  free smtp api for transactional email
  email api no credit card free tier

定时任务
  free cron job serverless
  scheduled edge functions

监控
  free uptime monitoring keep project alive
  free web analytics privacy friendly
```

**检索技巧**

- **加时间过滤**：GitHub 搜索用 `pushed:>2026-01-01` 过滤停更项目；网页搜索时优先看近 6 个月的内容。
- **加规模约束**：把「免费」「free tier」「no credit card」写进查询，能直接滤掉按量计费的服务。
- **加地域约束**：中文社区搜「大陆 可用性」「访问速度」，英文社区搜 `China accessibility`、`behind GFW`，两边都要看。
- **查额度看**：不只是「多少量」，更要看**限流条款、休眠规则、商业使用限制**（Vercel Hobby 的非商业定位、Supabase 的 7 天休眠都是这类条款）。
- **查锁定**：搜 `X vendor lock-in`、`migrate off X`，看是否有官方导出工具与社区迁移案例。
- **查活跃度**：GitHub 上看 last commit、issue 关闭速度、release 频率、是否只剩一个维护者。
- **看许可证**：MIT/Apache-2.0 可用；SSPL/BUSL 等要注意商业用途限制。

### 14.5 评估维度与打分表

给每个候选按下面维度打分（1–5 分），加权求和。**权重按本项目特点调整过** —— 大陆可访问性与免费额度权重最高，因为它们直接决定能不能用。

| 维度 | 权重 | 说明 | 5 分的样子 |
|---|:--:|---|---|
| 免费额度是否覆盖预期规模 | 20% | 单届 60 作品 / 400 用户 / 数千次请求 | 有数量级余量 |
| **中国大陆可访问性** | 20% | 延迟、丢包、是否被污染 | 有国内节点或大陆实测稳定 |
| 迁移与锁定成本 | 15% | 能否导出数据、是否开源可自托管 | 开源 + 官方导出工具 |
| 与 Vue 3 + Vite 的适配度 | 10% | 有无官方 SDK、社区示例 | 官方 JS SDK + 类型定义 |
| 维护活跃度与社区 | 10% | 迭代频率、问题响应 | 每周有提交，社区活跃 |
| 自托管可能性 | 10% | 未来能否搬走 | 开源且有一键部署方案 |
| 上手与文档质量 | 10% | 学习成本 | 文档有完整教程与示例 |
| 安全与合规（数据出境） | 5% | 是否涉及个人信息出境 | 有合规说明与数据处理条款 |

**打分表模板**（可直接复制使用）

| 候选 | 免费额度 | 大陆访问 | 锁定 | 适配度 | 活跃 | 自托管 | 文档 | 合规 | 加权总分 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| （填 1–5 分） | | | | | | | | | |

### 14.6 按能力缺口的候选工具横向对比

> 以下「选定」列的判断基于第 9 章的选型结果；其余为备选，供你日后重估。

#### A. 静态托管

| 候选 | 免费额度要点 | 大陆可访问性 | 备注 |
|---|---|---|---|
| **Cloudflare Pages**（选定） | 500 构建/月、静态带宽不限量、Functions 100k 请求/天 | ⚠️ 一般，`pages.dev` 间歇性超时 | 无出网费，最贴合本项目 |
| Vercel | Hobby 免费，但有带宽软限制 | ⚠️ 一般 | **免费档定位为非商业个人用途**，团队赛事有合规风险 |
| Netlify | 免费档 100 GB 带宽/月 | ⚠️ 一般 | 上手最快，额度较小 |
| GitHub Pages | 1 GB 仓库、100 GB 带宽/月 | ⚠️ 一般，部分反馈好于 `pages.dev` | **不能自定义 HTTP 响应头**，无法设缓存策略与安全头 |
| 阿里云 OSS + CDN / 腾讯云 COS + CDN | 有免费试用额度 | ✅ 最好 | 需**域名备案**，非「零运维」，是 12.1 方案 C/E 的落地路径 |
| EdgeOne Pages 等国内厂商静态托管 | 有免费档 | ✅ 好 | 国内厂商新方案，需核实免费条款与备案要求 |

#### B. 后端 / 数据

| 候选 | 模型 | 免费额度要点 | 锁定程度 | 备注 |
|---|---|---|---|---|
| **Supabase**（选定） | Postgres + Auth + Storage + Functions | 500 MB 库、1 GB 存储、50k MAU、单文件 50 MB、**7 天休眠** | 低（开源可自托管） | 一站式，RLS 是核心优势 |
| Appwrite Cloud | 类文档型 + Auth + Storage | 有免费档 | 低（全开源可自托管） | 数据模型不如 Postgres 贴合聚合查询 |
| Firebase | 文档型 + Auth + Storage | Spark 免费档 | 高 | 实时能力强，但聚合与唯一约束不自然 |
| PocketBase | SQLite 单二进制 | **无托管免费档**，只可自托管 | 无 | 极轻量，适合愿意自运维时；单机不适合多地域 |
| Neon / Turso | 纯数据库（PG / SQLite） | 有免费档 | 低 | 只解决数据库，鉴权与存储仍需另配 |
| Cloudflare D1 + Workers + R2 | 边缘 SQL + 计算 + 对象存储 | 有免费额度 | 中 | 全在同一家，出网免费；但边缘 SQL 限制多、无内置 OAuth |

#### C. 鉴权 / 登录

| 候选 | 说明 |
|---|---|
| **Supabase Auth（GitHub Provider）**（选定） | 内置，开箱即用，与数据库 RLS 天然联动 |
| giscus 式自建 OAuth 中转（Cloudflare Workers / 边缘函数） | 灵活可控，但 CSRF、state 加密、开放重定向都要自己处理，**不推荐** |
| Auth.js / NextAuth | 生态大，但更贴合 Next.js，Vue 下需自写适配 |
| Clerk / Auth0 | 体验好、有免费档，但 MAU 超出后较贵，且引入第三方身份数据出境 |
| Logto / Zitadel / Keycloak / Authentik | 开源可自托管，适合有服务器时；本项目「零运维」原则下不合适 |

#### D. 评论

| 候选 | 说明 |
|---|---|
| **giscus** | 基于 GitHub Discussions，零后端、无广告、与 GitHub 身份一致；**评论数据在 GitHub 仓库里**，与作品数据分离 |
| **Supabase 自建评论表** | 与作品数据同库，管理统一；需自做防滥用、限流、审核 |
| utterances | 基于 GitHub Issues，更简单但功能少 |
| Waline / Artalk / Twikoo / Cusdis | 开源，需自托管或使用其托管服务；大陆访问视部署位置而定 |
| Staticman | 把评论提交成 PR，流程重，不推荐 |

#### E. 邮件

| 候选 | 免费额度 | 备注 |
|---|---|---|
| **Resend**（选定） | 3,000 封/月，约 100/天 | 无品牌页脚、免信用卡、API 简洁 |
| Brevo | 300 封/天 | 邮件带品牌页脚 |
| Mailgun | 100 封/天 | 需信用卡，日志保留 1 天 |
| Postmark | 仅试用 | 送达率极佳但无长期免费档 |
| Amazon SES | 按量计费（极便宜） | 非免费，但量小时成本可忽略 |
| 阿里云邮件推送 / 腾讯云 SES | 有免费额度 | 国内送达好，但需域名备案与实名 |

> 无论选哪家，都需要**自有域名 + 配置 SPF / DKIM / DMARC**，否则邮件进垃圾箱。

#### F. 定时任务 / 保活

| 候选 | 说明 |
|---|---|
| **Supabase Scheduled Edge Functions / `pg_cron`**（选定） | 与数据同平台，无需额外服务 |
| Cloudflare Cron Triggers | 适合拿来给 Supabase 发保活心跳 |
| UptimeRobot / cron-job.org | 免费外部定时器，可同时做保活与可用性监控 |

#### G. 前端配套

| 用途 | 候选 | 说明 |
|---|---|---|
| UI 组件 | Naive UI / Element Plus / PrimeVue / Vuetify | 全部支持按需引入与主题定制；本项目自写样式为主，仅在需要复杂控件时引入 |
| 图标 | Iconify + `unplugin-icons` | 按需打包，不会因为引入整套图标库而膨胀 |
| 主题切换 | 原生 CSS 变量 + `prefers-color-scheme` | 不必引入库 |
| Head 管理 | `@unhead/vue` | 动态 title / description / OpenGraph |
| 站点地图 | `vite-plugin-sitemap` 类插件 | 生成 `sitemap.xml` |
| 图片压缩 | 浏览器端压缩（Canvas / WASM）后再上传 | 省 Storage 额度最有效的手段 |
| 监控 | Cloudflare Web Analytics（免费） / Sentry（免费档） / Umami | 优先选无 Cookie、隐私友好的方案 |
| 代码质量 | ESLint + Prettier（仅开发期） | 不进入产物 |

### 14.7 用这个方法论复评当前选型的结论

把选定组合代入 14.5 的打分表，结论是：

- **Cloudflare Pages**：免费额度（5/5）、适配度（5/5）满分，但**大陆可访问性只有 2–3 分**，是整体方案的短板；
- **Supabase**：免费额度（4/5）、迁移成本（5/5，开源可自托管）、适配度（5/5）良好，但**大陆可访问性同样只有 2–3 分**，且 7 天休眠是额外扣分项；
- **Resend**：免费额度（5/5）、无品牌页脚，全维度表现均衡，风险最低；
- **giscus**：免费（5/5）、大陆可访问性（3/5，GitHub 本身在大陆访问不稳），但需要 GitHub 账号心智。

**综合判断**：当前组合是「功能与成本最优解」，但**大陆访问是系统性短板**，必须靠 12.1 的域名 / CDN 方案补足。若大陆可访问性被定为**硬性验收标准**，则应回到 14.6 的 A 与 B 表，重新评估「国内对象存储 + CDN + 备案域名」的组合，这会显著提高成本与运维投入。

### 14.8 保持工具清单不过期的做法

1. **建立自己的候选矩阵**（14.5 的模板），把每个候选的「额度、大陆实测延迟、最后核实日期」记下来；
2. **每届活动开始前重跑一遍核实**，重点核对官方定价页，而不是依赖本文档或任何文章；
3. **只用官方定价页做最终决策**，媒体文章与博客常滞后半年以上；
4. **给额度设告警阈值**（如 Storage 用到 70% 时提醒），避免活动中途触顶；
5. **保留迁移预案**：数据导出脚本 + 静态站可换托管，是成本最低的保险。

---

## 15. 附录 A：本次核实的来源与说明

| 主题 | 核实日期 | 来源类型 |
|---|---|---|
| Supabase 免费档额度与休眠规则 | 2026-09-15 | 官方文档 Pricing / Limits 页 |
| Cloudflare Pages 免费档额度 | 2026-09-15 | 官方文档 Limits 页 |
| Resend / Brevo / Mailgun / SendGrid 免费额度 | 2026-09-15 | 各官方 Pricing 页 |
| 静态站评论方案（giscus / utterances / 自托管对比） | 2026-09-15 | 项目官方仓库与文档 |
| BaaS 横向对比（Supabase / Firebase / Appwrite / PocketBase） | 2026-09-15 | 各官方文档 + 社区对比文章 |
| **中国大陆访问 Cloudflare Pages 的实测与反馈** | 2026-09-15 | 社区实测帖与讨论（河北、江苏等地超时案例） |

> ⚠️ 免责说明：本文档中的额度与可用性数字**均为核实当日的状态**，此类信息变动频繁。任何**付费决策或架构变更前**请重新访问官方定价页确认。

---

## 16. 附录 B：需求追溯表

将最初的 5 条需求映射到本文档章节，确保没有遗漏：

| 原始需求 | 对应章节 | 覆盖情况 |
|---|---|---|
| 1. 使用 GitHub 账户登录 | 3.3、6.1、10.2 | ✅ 仅 GitHub OAuth，经 Supabase Auth 实现 |
| 2. 上传或修改属于自己的作品 | 6.3、8.4 | ✅ 含权限隔离与投稿期限制 |
| 3. 在主页显示最近的活动 | 5.2、6.2 FR-2.1 | ✅ 主页最近活动卡片区 |
| 4. 活动页显示作品（封面图 + 标题 + 作者） | 5.3、6.4 FR-4.1 | ✅ 三要素严格落位 |
| 5. 管理员可添加活动、删除作品 | 6.6 FR-6.1 / FR-6.2 | ✅ 另扩展了下架、角色管理、审计 |
| （追加）QQ 登录 | 6.10 | ❌ 已按你的要求放弃 |
| （追加）三文件强制投稿包（`maidata.txt` + `bg.png`/`bg.jpg` + `track.mp3`） | 6.10 | ❌ 已放弃，改为「封面图 + 公开谱面外链」以减轻在线存储压力 |
| （追加）封面图 + 公开可访问的谱面下载外链 | 4.3、6.3、8.4、12.8 | ✅ **当前投稿形态** |
| （追加）外链须落在**域名白名单**内，提取码按域名条件必填 | 6.3、6.6 FR-6.11、8.9、17.12～17.15 | ✅ 管理员可扩展的清单表，投稿流程零网络探测 |
| （追加）giscus 式 GitHub 登录 | 3.3、9.2 | ✅ 借鉴范式，改用 Supabase Auth 落地 |

---

## 17. 决策记录（16 项已由你来拍板）

以下决策在 2026-09-15 的确认环节中**已逐条定案**。本节作为决策台账保留，供后续回溯「为什么这么设计」。相关章节已按结论同步更新。

- **首次确认（17.1 ～ 17.11）**：11 项。
- **同日二次修订**：**17.1 / 17.3** 随投稿形态翻转（三件套 → 封面图 + 外链）改写，17.4 随之不再适用。
- **同日三次追加（17.12 ～ 17.16）**：投稿形态确定后新暴露的 5 项问题——外链**怎么校验**、白名单**谁维护**、提取码**怎么收**、未命中**怎么引导**、封面**多大**。

| # | 问题 | 你的决定 |
|---|---|---|
| 17.1 | 投稿形态（原名「三件套上传形式」） | **封面图 + 公开可访问的谱面下载外链**（一个文件控件 + 一个 URL 输入框）〔二次修订〕 |
| 17.2 | 参赛活动单选还是复选 | **单选**（下拉框），一件作品只投一个活动 |
| 17.3 | 文件名校验与体积上限 | **封面图无文件名要求**，png/jpg/jpeg/webp；谱面走外链，不计体积〔二次修订，上限另见 17.16〕 |
| 17.4 | 是否提供在线试听 | **不再适用**，平台不再持有音频文件〔二次修订〕 |
| 17.5 | 作者能否自评 | **允许**，排行榜标注「含作者评分」 |
| 17.6 | 评分能否随时改 | **可随时修改、不限次数**，评分截止后锁定 |
| 17.7 | 评论方案 | **自建评论表**（`comments`），不引入 giscus |
| 17.8 | 未登录访客可见范围 | **作品墙与排行榜公开，作品详情页需登录** |
| 17.9 | 中英双语 | **单语中文**，不引入 i18n |
| 17.10 | 投稿截止后能否改作品 | **完全锁定**，本人也不可改 |
| 17.11 | 首发是否绑自定义域名 | **先不绑定**，用 `*.pages.dev` 起步，保留随时绑定 |
| 17.12 | 外链怎么校验 | **域名白名单**：命中即放行，**不做任何可达性网络探测**〔三次追加〕 |
| 17.13 | 白名单谁维护 | **数据库表 `link_domains` + 管理员后台可增删**，并预置种子清单；支持整域 / 精确主机两种粒度〔三次追加〕 |
| 17.14 | 提取码怎么收 | **选填字段，按域名条件必填**：仅当该域名被标记为「需密码」时才要求〔三次追加〕 |
| 17.15 | 白名单未命中怎么办 | **拒绝提交 + 友好提示 + 给出口**（展示已放行清单与推荐教程 + 申请添加托管平台）〔三次追加〕 |
| 17.16 | 封面图体积上限 | **1 MB**（比初版建议的 2 MB 更紧）〔三次追加〕 |

### 17.1 投稿形态 → 封面图 + 公开可访问的谱面外链

> **2026-09-15 同日二次修订**：本节原结论为「三件套（`maidata.txt` + `bg.png`/`bg.jpg` + `track.mp3`）三分文件分别上传」。修订后，投稿内容收敛为**一张封面图 + 一个公开可访问的谱面下载外链**。

**修订理由**：三件套方案把谱面文本与音频都留在平台侧，按 11.4 测算，单届 60 件作品约占用 660 MB 存储，**免费档 1 GB 额度成为最先逼近的瓶颈**（利用率 66%）。改为外链后，平台侧只剩封面图，单届约 60 MB（利用率 6%），Storage 不再是瓶颈；音乐与谱面的存储与流量成本转移给作者自选的公共托管（GitHub Release、网盘、对象存储等）。

**为什么不做「选手自己打包 zip」**：即便在旧的三件套方案下也已被否决——分别校验能**逐项**给出错误提示，而 zip 需要服务端解包，还要额外防 zip 炸弹与路径穿越。

**落地要求**
- 表单呈现**一个图片文件控件**（封面图，带预览）+ **一个 URL 输入框**（谱面下载外链，带「该托管平台是否已支持」就地回显）+ **一个条件出现的提取码输入框**；
- 封面图是平台侧**唯一**需要上传的文件，因此 Storage bucket 只保留封面图与活动封面两类对象；
- 谱面外链必须为 `http`/`https` 绝对 URL，且主机名必须命中**域名白名单**（见 17.12、6.3）；
- **平台不得代理下载或转存外链内容**，详情页只做 302 跳转。

### 17.2 参赛活动 → 单选（下拉框）

原需求写的是「复选框」，但复选会带来两个问题：排行榜按届统计，同一作品会在多届重复出现；若两个活动的评分期重叠，同一条评分算不算数也说不清。因此改为**下拉框单选**。

**落地要求**：下拉框只列出状态为「投稿+评分中」的活动；`works` 表保持 `event_id` 外键，不需要额外的「作品 ↔ 活动」关联表。

### 17.3 封面图校验与体积上限 → 无文件名要求，≤ 1 MB

| 项目 | 规则 |
|---|---|
| 文件名 | **无要求**，用户上传时是什么名字就存什么名字（落库时统一重命名为 UUID 存储路径） |
| 允许格式 | `png` / `jpg` / `jpeg` / `webp` |
| 体积上限 | **1 MB**（见 17.16） |
| 谱面文件 | 不经过平台，无体积限制 |

- **取消文件名约束的原因**：旧方案之所以要求 `bg.png`/`bg.jpg`，是为了让三件套在下载后仍能被打包脚本识别；改为外链后平台只存一张封面，而封面**只用于页面展示**，不参与任何后续处理，因此再无收敛文件名的必要。强制文件名只会给用户制造无谓的困惑（Windows 拖拽时系统还会自动改写扩展名显示）。
- **格式校验采用三重一致**：文件扩展名 + 浏览器上报的 MIME + 文件头魔数，三者必须一致，防止把 `.exe` 改名成 `.png` 绕过（见 6.3）。
- **体积上限由 2 MB 收紧到 1 MB**（17.16）：封面仅用于列表卡片（约 400×300）与详情页头部（约 1200×900），转 WebP 后通常不到 300 KB。按 11.4 测算，单届 60 张封面最多占约 **60 MB**。
- **Storage 不再是瓶颈**：结论已同步至 11.4 与 12.3；风险从「存储不够」转移到「外链外部依赖」，见 12.8。

### 17.4 在线试听 → 不再适用

旧的「三件套」方案里还有 `track.mp3`，因此需要回答「要不要提供在线试听」。**改为外链形态后，平台侧已不存在任何音频文件**，本节问题自然消失，无需再做取舍。

- 平台既不提供播放器，也不代理音频流量，出网流量（免费档 5 GB/月）不会被听众消耗；
- 作品详情页只出现「前往下载谱面」按钮，跳转至作者提供的外部链接；
- 若后续确有试听需求，应作为**独立议题**重新评估（涉及是否引入第三方播放器、是否允许嵌入音频直链），不在当前范围内。

### 17.5 作者自评 → 允许，但需标注

允许作者给自己的作品打分，**不做「评分人 ≠ 投稿人」的强制约束**。为防止观感上的不公平，**排行榜必须对含作者自评的作品标注「含作者评分」**。

**落地要求**：榜单查询时顺带判断「是否存在 `ratings.user_id = works.user_id` 的记录」，有则在作品行展示角标。此项写入 FR-5.4 / FR-5.8。

### 17.6 评分修改 → 不限次数，截止后锁定

评分期内可随时修改（重复提交即覆盖），**不限次数**；**评分截止（⑤）后完全锁定**，任何增删改都被后端拒绝。

**落地要求**：后端所有评分写操作前置判断 `now() < events.rating_closes_at`；前端在系统时间超过截止点后直接隐藏评分控件。

### 17.7 评论 → 自建评论表

采用自建的 `comments` 表（见 8.6），与作品数据同库；**不引入 giscus**。好处是管理后台能统一审核与删除评论，且评论与作品的外键关联完整保留。

**落地要求**：按 12.5 的同类手段做限流与防滥用；`hidden` 字段支撑「隐藏而非物理删除」。

> 附：9.3 的「评论」一行已同步改为「Supabase 自建评论表」。giscus 在本项目中**仅作为 GitHub OAuth 范式的参考**保留在 3.3 / 9.2，不再作为评论实现方案。

### 17.8 访客可见范围 → 作品墙与排行榜公开，详情页需登录

- **公开**（无需登录）：主页、活动列表、活动页、**作品墙**、**排行榜**、投稿须知；
- **需登录**：**作品详情页**、投稿、评分、评论。

这样既能靠公开的作品墙做传播，又能把「下载、评分、评论」这类深层交互统一留在登录态之后，便于统计与治理。

**落地要求**：`/works/:id` 加入路由守卫（见 5.1 已改为「需要登录 ✅」）；未登录点开详情时，先展示作品摘要与「登录后查看完整内容与下载」引导，登录后回到原地址。

**需要留意的取舍**：分享到聊天软件的详情页链接，外人点开会看到登录引导页而非作品内容——OpenGraph 预览图（FR-4.6）仍可正常出图，但**预览描述文案要写成「登录后查看」**，避免观感上的「打不开」。

### 17.9 语言 → 单语中文

面向校内中文用户，**不引入 i18n 方案**，UI 文案直接以中文硬编码，省掉一层抽象与翻译维护成本。

### 17.10 投稿截止后的修改权 → 完全锁定

投稿截止（④）后，**选手本人也无法修改或删除自己的作品**，避免「截止后偷换文件」的不公平。例外通道只有管理员，且必须写入 `audit_logs`（见 8.8）。

**落地要求**：`works` 的更新与删除策略都要以 `now() < events.submit_closes_at` 作为必要条件；不只在前端隐藏按钮。

### 17.11 自定义域名 → 首发先不绑定

先用 `*.pages.dev` 起步，零配置上线；**保留随时绑定自定义域名**的能力。域名是 12.1 中「马上能做」的第一档缓解手段，等站点跑起来、确认要长期运营后再绑定。

**待办提醒**：绑定域名时要在域名商处添加 CNAME 指向 Cloudflare Pages，并回 GitHub OAuth App / Supabase 的授权回调地址里补上新域名。

### 17.12 外链怎么校验 → 域名白名单，不做网络探测

原方案是「格式硬校验 + 可达性**软校验**」：提交时由服务端探测一次链接，探测失败不阻断、只做提示。**本次改为域名白名单**：

- **命中白名单 → 立即放行**，不发起任何网络请求；
- **未命中 → 明确拒绝**，并给出替代方案（见 17.15）。

**为什么换掉软校验**
1. **探测结果不可靠**：国内网盘对服务端探测普遍返回 403 或直接拒绝连接，探测失败 ≠ 链接不可用；反过来探测成功也证明不了链接三天后还在。
2. **探测有成本与副作用**：服务端出网、超时等待、可能被目标站点识别为爬虫而限速或封 IP。
3. **白名单是确定性的**：同样的输入永远得到同样的判定，选手不会遇到「同样一个链接，今天能投明天不能投」。

**代价与应对**：新增托管平台需要管理员先加域名。为此把白名单做成**可维护的数据表**（17.13），后台一键增删，无需改代码重新部署；同时在投稿页给出「申请添加托管平台」入口（17.15），避免这道门槛变成死角。

**落地要求**：投稿流程（前端 + 后端）**不含任何对外链的网络请求**；`chart_link_status` 的探测只保留在每周定时巡检里（见 10.6）。校验逻辑收在**一处共享实现**（正则解析主机名 → 归一化 → 查白名单），前端与后端复用同一份规则，避免判定不一致。

### 17.13 白名单谁维护 → 数据库表 + 后台可增删 + 预置种子清单

新增 `link_domains` 表（见 8.9），并预置一份覆盖主流平台的种子清单。

- **支持两种粒度**：`exact`（精确主机名，如 `pan.baidu.com`）与 `suffix`（该域及其所有子域，如 `lanzou.com`），覆盖网盘经常更换备用域的情况；
- **管理员可在后台增删、启停、排序**（FR-6.11），所有变更写入 `audit_logs`；
- **停用不影响历史作品**：`enabled = false` 只阻止新投稿，已上墙的作品照常展示（因此 `works` 与 `link_domains` 之间**不做外键**，见 8.10）；
- **种子清单范围**（你的决定）：代码托管 / 发布类（GitHub、Gitee、GitLab、jsDelivr、raw）与对象存储直链，加上主流网盘（百度、蓝奏云、123 云盘、阿里云盘、天翼云盘、夸克、OneDrive、Google Drive、MEGA、Dropbox）。**不含**临时文件托管与短链服务。

**落地要求**：种子清单随首次迁移写入，保证首发当天就能投稿；后台提供一个「已放行平台清单」只读视图，供投稿页与帮助页复用同一份数据。

### 17.14 提取码 → 选填字段，按域名条件必填

白名单里既有直链型平台（GitHub Release、对象存储，**无需密码**），也有分享页型平台（网盘，**可能需要密码**）。因此：

- 表单**新增「提取码 / 密码」字段**，随外链存在而出现；
- **只有当该域名在 `link_domains` 中被标记为「需密码」时，该字段才变为必填**；直链型域名下保持选填（默认折叠，提供「有提取码？」展开链接）；
- 长度 1–16 位，纯文本，**属于非敏感信息**（它只对已有链接的人有意义，且本来就随分享链接一起公开传播）；
- 详情页（FR-4.2）在下载按钮旁显示提取码并提供**一键复制**，避免用户下载时再回头翻作品说明。

**落地要求**：`works.chart_link_password` 存明文，不加额外加密层——它与 `chart_link` 同级的公开信息；后端按域名标记做**条件必填**校验，不能只靠前端控制。

### 17.15 白名单未命中 → 拒绝提交，但给出三条出路

白名单机制最大的体验风险是「选手手里有一个平台，但平台不认识它」。因此未命中时的处理必须是**拒绝 + 解释 + 出路**三件套，而不是一句「链接无效」：

1. **友好提示**：文案写「暂不支持该托管平台」，并点明当前只支持哪些类型（代码发布 / 对象存储 / 主流网盘）；
2. **同页展示已放行清单**：直接列出 `link_domains` 中启用的平台名与域名（数据同源，管理员加一个域名，这里立刻出现），并附**推荐托管教程**——首选「GitHub Release 上传 + 复制直链」，其次「网盘分享页 + 提取码」；
3. **申请添加入口**：提供「申请添加托管平台」链接（邮件或 GitHub Issue 模板），管理员评估后加入白名单。

**落地要求**：投稿页不得只说「域名不在白名单内」——这种提示会让选手完全不知道下一步该做什么。申请入口的落地方式（邮件地址 / Issue 模板地址）需要在第 18 章的手动配置阶段确定。

### 17.16 封面图体积上限 → 1 MB

从初版建议的 2 MB **收紧到 1 MB**。理由与影响：

- 封面只用于 400×300 的卡片与 1200×900 的详情页头部，**转 WebP 后通常不到 300 KB**，1 MB 对正常投稿足够宽松；
- 单届 60 张封面上限从 120 MB 降到 **60 MB**，Storage 利用率 6%，额度问题彻底退出视野（见 11.4）；
- **需要注意的副作用**：未压缩的高质量 PNG（尤其是截图直出）很容易超过 1 MB。因此投稿页的选图校验必须给出**可操作的压缩指引**（推荐 WebP、质量 80，附在线压缩工具链接），并在后端再拦一次（见 12.3）；
- 上限是后台可调的配置项，若实际投稿中压缩门槛过高，可随时调回 2 MB。

---

## 18. 下一步（等你的指令）

本文档已定稿。后续工作按 13 章的里程碑推进，其中 **M0 之前需要你手动完成的外部配置**：

| # | 事项 | 在哪做 | 备注 |
|---|---|---|---|
| 1 | 创建 Supabase 项目，拿到 Project URL 与 anon key | supabase.com 控制台 | 免费档，需邮箱注册 |
| 2 | 在 Supabase 中启用 GitHub Provider，填 Client ID / Secret | Supabase Auth 设置 | —— |
| 3 | 创建 GitHub OAuth App，回调地址填 Supabase 的回调地址 | github.com/settings/developers | **不是**填本站地址 |
| 4 | 创建 Cloudflare 账号并连上 GitHub 仓库，建 Pages 项目 | dash.cloudflare.com | 构建命令 `npm run build`，输出 `dist` |
| 5 | 把 Supabase 的公开变量填进 Cloudflare Pages 的环境变量 | Pages 设置 | 只填 `VITE_` 前缀的公开值 |
| 6 | 首个管理员引导（见 10.4） | Supabase 数据库面板 | 手动把自己设为 `admin`，仅一次 |
| 7 | 建 Resend 账号，拿 API Key（邮件通知用，见 11.3） | resend.com | 可延后到 FR-8 实现时再做 |
| 8 | 确定「申请添加托管平台」的联络方式（邮箱地址或 Issue 模板链接）（见 17.15） | 你决定 | 填进站点配置即可，随时可改 |

> 以上 8 项均为**人工在外部平台 / 你的决策**，我在本地无法代劳；但可以逐项给出详细的图文步骤与要填的具体值。

---
